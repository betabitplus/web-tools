Examples
========

Task-sized live examples built only on the public ``web_tools`` API. Each page
keeps the core call close to the observable result and separates setup from the
behavior being demonstrated.

The examples use real pages, browser automation, and media downloads when executed.
Required CI imports and type-checks them without running the live workflows.
